import type { ModelConfig } from './types';

export const modelConfig: ModelConfig = {
  canvasWidth: 1180,
  canvasHeight: 720,
  observationHeight: 150,
  layerHeight: 92,
  layerGap: 18,
  leftPadding: 72,
  rightPadding: 164,
  zeroAxisDash: [8, 7],
  uploadParticleCount: 9,
};
